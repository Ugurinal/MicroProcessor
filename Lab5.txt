1-----------------------------------------------------------------------------------------------------------------------------------------
mov ax, 0100h
mov es, ax
mov di, 300h   


mov ax, 0BCDEh

mov cx, 80h  // (3ff - 300 ) / 2 kadar yaziyoruz cunku word word gidiyoruz yarisi kadar gitmemiz gerekiyor

rep stosw

ret
------------------------------------------------------------------------------------------------------------------------------------------
